% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mood-median-test.R
\name{MEDIAN_TEST}
\alias{MEDIAN_TEST}
\title{Mood's Median Test}
\usage{
MEDIAN_TEST(.var_id = NULL, .data = NULL, ...)
}
\arguments{
\item{.var_id}{A variable mapper \verb{<var_id>}. Supports \code{x_by()} and
\code{on()}. When supplied, the test executes immediately. \code{x_by()} with
more than one grouping variable requires \code{via("multi")}; see the
\strong{Supported variable mapper} section.}

\item{.data}{A data frame. Only used on the standalone path.}

\item{...}{Additional arguments passed to the implementation:
\code{custom_median} (a hypothesized median to split on, instead of the
sample's own grand median), accepted on every path, and \code{display_ct}
(show the contingency table in \code{print()}; default \code{FALSE}), accepted
on \code{base} (both \code{x_by()} and \code{on()}). \code{x_by()}'s \code{multi} variant
instead accepts \code{display_var} — an index or grouping-variable name
choosing which variable's table to compute and show (\code{FALSE} to skip
it) — since with several grouping variables there's a choice to make
that \code{base}/\code{on()} never have. See the \strong{Supported variable mapper}
section for the full list per path.}
}
\value{
A \code{cld_exec} object (in \code{\link[statim:conclude]{statim::conclude()}}), a \code{stat_infer_spec}
object, or a \code{test_spec} when \code{.var_id = NULL}. \code{mmdtest_def_xby}'s
\code{base} and \code{multi}, and \code{mmdtest_def_on}'s baseline, all return a
\link{class_median_test} object. On \code{multi}, every grouping variable's test
runs and its \code{statistic}/\code{df}/\code{p_value}/\code{median} are kept, but only
the \code{display_var}-selected variable's contingency table is retained
in \code{cont_tab} — the others' tables are discarded. On the \code{on()} path,
\code{vars} is fixed to \code{"on"} since there is no grouping variable to
label.
}
\description{
\code{MEDIAN_TEST()} tests whether a continuous variable's population median
differs across the levels of one or more grouping variables. It works by
dichotomizing all observations at the grand median (or a supplied
\code{custom_median}) and running a chi-squared test of independence on the
resulting 2xk contingency table. If \code{MEDIAN_TEST} is supplied within the
lazy-loaded pipeline, supply \code{MEDIAN_TEST} as a function i.e.
\code{prepare_test(.test = MEDIAN_TEST)}.
}
\details{
\code{H0}: all groups share a common population median. \code{H1}: at least one
group's median differs from the others.
}
\section{Supported variable mapper \verb{<var_id>}s}{

\itemize{
\item \code{x_by()}: grouped Mood's median test. The \code{base} path handles exactly
one grouping variable; the \code{multi} variant handles more than one,
running one test per grouping variable. See details from
\link{mmdtest-xby}.
\item \code{on()}: one-sample or k-sample Mood's median test via a compiled
backend, run directly on \code{.proc$data} without an \code{x_by()} split. See
details from \link{mmdtest-on}.
}
}

\section{A note on \code{custom_median} for single-group tests}{

With exactly one group and no \code{custom_median} supplied, the split point
is estimated from that same group's data, so the above/below counts land
near a 50/50 split by construction and the test has little power to
detect anything. Supply \code{custom_median} (a hypothesized population
median) whenever you're testing a single group.
}

\examples{
set.seed(123)
x = rcauchy(50, 1, 1.5)
g = sample(letters[1:5], size = 50, replace = TRUE)
MEDIAN_TEST(x_by(x, g))

# against a hypothesized median rather than the sample's own
MEDIAN_TEST(x_by(x, g), custom_median = 1)

}
\seealso{
\link{mmdtest-xby}, \link{mmdtest-on}, \link{class_median_test}, \code{\link[statim:via]{statim::via()}}, \code{\link[statim:conclude]{statim::conclude()}}
}
