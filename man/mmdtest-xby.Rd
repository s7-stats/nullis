% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mood-median-test-xby.R
\name{mmdtest-xby}
\alias{mmdtest-xby}
\title{Mood's Median Test: Grouped (\code{x_by})}
\description{
The \code{x_by} implementation tests whether a continuous variable's
population median differs across the levels of a grouping variable.
}
\section{Arguments}{

\code{mmdtest_def_xby}'s baseline \code{fn} takes \code{.proc} plus an optional
\code{custom_median} forwarded through \code{...} in \code{\link[=MEDIAN_TEST]{MEDIAN_TEST()}}.
}

\section{Variants}{

\describe{
\item{\code{"multi"}}{Runs one Mood's median test per grouping variable
when \code{group} from \code{x_by()} selects multiple variables. Accepts \code{custom_median}
(applied identically to every grouping variable's test) and
\code{display_var}, a 1-based index (default \code{1L}, the first grouping
variable) choosing which grouping variable's contingency table is
computed and shown; pass \code{display_var = FALSE} to skip it entirely.
Only the selected variable's table is retained on the returned
object, so re-run with a different \code{display_var} to inspect another
one.}
}
}

\section{Grouped Mood's Median Test default class}{

Both \code{base} and \code{multi} return a \link{class_median_test} object.
\code{base} always has one grouping variable, so \code{cont_tab} holds its one
table. \code{multi} has as many \code{statistic}/\code{df}/\code{p_value} entries as
grouping variables, but \code{cont_tab} holds only the one \code{display_var}
selected (or none, if \code{display_var = FALSE}).
}

\examples{
box::use(
    statim[define_model, prepare, via, conclude]
)

set.seed(123)
x = rcauchy(50, 1, 1.5)
g1 = sample(letters[1:5], size = 50, replace = TRUE)
g2 = sample(c("control", "treatment"), size = 50, replace = TRUE)

MEDIAN_TEST(x_by(x, g1))

# show g1's contingency table
define_model(x_by(x, g1)) |>
    prepare(MEDIAN_TEST) |>
    conclude()

# more than one grouping variable requires "multi"; shows g2's table
define_model(x_by(x, c(g1, g2))) |>
    prepare(MEDIAN_TEST) |>
    via("multi", display_var = 2L) |>
    conclude()

}
\seealso{
Other mmdtest-implementations: 
\code{\link{mmdtest-on}}
}
\concept{mmdtest-implementations}
\keyword{internal}
