% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jt-test.R
\name{class_jt_test}
\alias{class_jt_test}
\title{Structured result container for the Jonckheere-Terpstra test}
\description{
An S7 class produced by \link{JT_TEST} pipelines using \link[statim:x_by]{statim::x_by} as the
variable mapper \verb{<var_id>}.

Inherits from \link[statim:class_stat_infer]{statim::class_stat_infer}, so \code{\link[statim:auto_tidy]{statim::auto_tidy()}}
dispatches on it automatically. Downstream packages can use it as a
\code{parent} in \code{S7::new_class()}.
}
\details{
Only \code{vars}, \code{statistic}, \code{p_value}, and \code{alternative} are guaranteed
by every constructor of \code{class_jt_test}. \code{z_score}, \code{mean}, and
\code{variance} are optional and, when supplied, join the headline summary
table on \code{print()}; when absent, they're left out of that table
entirely rather than shown as \code{"Not given"}. \code{approximate} and
\code{method} are also optional, and appear in a second details table,
printed as \code{"Not given"} when a constructor doesn't supply them. The
\code{x_by} baseline path always populates all eight, since
\code{jonckheere_terpstra_test()} returns them unconditionally.
}
