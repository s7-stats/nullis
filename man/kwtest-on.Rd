% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kruskal-test-defs-on.R
\name{kwtest-on}
\alias{kwtest-on}
\title{k-independent Samples Kruskal-Wallis Test (\code{on})}
\description{
The \code{on} implementation runs a Kruskal-Wallis test via a compiled C++
backend (\code{kruskal_wallis_cpp()}) directly on \code{.proc$data}, without going
through an R-level \code{x_by()} grouping split.
}
\section{Arguments}{

\code{kwtest_def_on}'s baseline \code{fn} takes no arguments beyond \code{.proc} —
nothing is passed through \code{...} in \code{\link[=KW_TEST]{KW_TEST()}} or \code{\link[statim:via]{statim::via()}} for this path.
}

\section{Variants}{

None. \code{kwtest_def_on} declares only a \code{base} baseline — no \code{variant()}
entries.
}

\section{k-independent Samples Kruskal-Wallis default class}{

No S7 wrapper. The baseline returns whatever \code{kruskal_wallis_cpp()}
returns, unwrapped, a plain list with \code{statistic}, \code{df}, \code{p_value}.
Unlike \code{x_by()}, this path does \strong{not} return \link{class_kw_test}. It has
its own \code{print} method (a \code{tibble} with \verb{H Statistic},
\verb{Degrees of Freedom}, \code{p-value} columns) and its own registered
\code{\link[statim:making_tidy]{statim::making_tidy()}} entry (\code{tibble::as_tibble(.x@data)}), rather
than inheriting \code{\link[statim:auto_tidy]{statim::auto_tidy()}} from \code{class_kw_test}.
}

\examples{
set.seed(123)
x = rcauchy(50, 1, 1.5)
y = rcauchy(50, 3, 1.5)
KW_TEST(on(x, y))

}
\seealso{
Other kwtest-implementations: 
\code{\link{kwtest-xby}}
}
\concept{kwtest-implementations}
\keyword{internal}
