% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jt-test.R
\name{JT_TEST}
\alias{JT_TEST}
\title{Jonckheere-Terpstra Test}
\usage{
JT_TEST(.var_id = NULL, .data = NULL, ...)
}
\arguments{
\item{.var_id}{A variable mapper \verb{<var_id>}. Currently supports \code{x_by()}.
When supplied, the test executes immediately. If \code{.var_id} maps
multiple grouping variables, one Jonckheere-Terpstra test runs per
grouping variable against the same continuous variable. Each grouping
variable must be an ordered factor — see \link{jttest-xby}.}

\item{.data}{A data frame. Only used on the standalone path.}

\item{...}{Additional arguments passed to the implementation, including
\code{alternative} and \code{approximate}. See \link{jttest-xby} for the full list.}
}
\value{
A \code{cld_exec} object (in \code{\link[statim:conclude]{statim::conclude()}}), a \code{stat_infer_spec}
object, or a \code{test_spec} when \code{.var_id = NULL}. \code{jttest_def_xby}
always returns a \link{class_jt_test} object.
}
\description{
\code{JT_TEST()} tests whether the distribution of a continuous variable
shifts monotonically across the \emph{ordered} levels of a grouping
variable. It is a trend-sensitive alternative to Kruskal-Wallis: where
Kruskal-Wallis only asks whether the groups differ, Jonckheere-Terpstra
asks whether they differ in a consistent direction. If \code{JT_TEST} is
supplied within the lazy-loaded pipeline, supply \code{JT_TEST} as a function
i.e. \code{prepare_test(.test = JT_TEST)}.
}
\details{
\code{H0}: all groups come from the same distribution. \code{H1}: the groups are
stochastically ordered in the direction given by \code{alternative}.
}
\section{Supported variable mapper \verb{<var_id>}s}{

\itemize{
\item \code{x_by()}: grouped Jonckheere-Terpstra test against an ordered
grouping variable. See details from \link{jttest-xby}.
}
}

\examples{
set.seed(123)
x = rcauchy(50, 1, 1.5)
g = factor(
    sample(letters[1:3], size = 50, replace = TRUE),
    levels = c("a", "b", "c"),
    ordered = TRUE
)
JT_TEST(x_by(x, g))

# direction of the trend matters here, unlike Kruskal-Wallis
JT_TEST(x_by(x, g), alternative = "increasing")

# multiple grouping variables -> one test per grouping variable
# (confirm this call shape against your actual x_by() signature)
g2 = factor(
    sample(c("low", "high"), size = 50, replace = TRUE),
    levels = c("low", "high"),
    ordered = TRUE
)
JT_TEST(x_by(x, c(g, g2)))

}
\seealso{
\link{jttest-xby}, \link{class_jt_test}, \code{\link[statim:via]{statim::via()}}, \code{\link[statim:conclude]{statim::conclude()}}
}
