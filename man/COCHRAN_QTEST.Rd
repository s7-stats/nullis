% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cochran-test.R
\name{COCHRAN_QTEST}
\alias{COCHRAN_QTEST}
\title{Cochran's Q test}
\usage{
COCHRAN_QTEST(.var_id = NULL, .data = NULL, ...)
}
\arguments{
\item{.var_id}{A variable mapper \verb{<var_id>}. Currently supports \code{x_by()}.
When supplied, the test executes immediately. If \code{.var_id} maps
multiple grouping variables, one Kruskal-Wallis test runs per grouping
variable against the same continuous variable.}

\item{.data}{A data frame. Only used on the standalone path.}

\item{...}{Additional arguments passed to the implementation. See the
\strong{Supported variable mapper} section for the full list per path.}
}
\value{
A \code{cld_exec} object (in \code{\link[statim:conclude]{statim::conclude()}}), a \code{stat_infer_spec}
object, or a \code{test_spec} when \code{.var_id = NULL}. \code{cqtest_def_xby}'s
baseline returns a \link{class_cq_test} object by default; its \code{pairwise}
variant instead returns a plain list (\code{COCHRAN_QTEST}, \code{comps}) with its own
\code{print} method. \code{cqtest_def_on}'s baseline returns a plain list
(\code{statistic}, \code{df}, \code{p_value}) with its own \code{print} method — neither
path shares a class between them.
}
\description{
\code{COCHRAN_QTEST()} tests whether the distribution of a continuous variable
differs across the levels of one or more grouping variables. It is the
rank-based, distribution-free analogue to one-way ANOVA. If \code{COCHRAN_QTEST} is
supplied within the lazy-loaded pipeline, supply \code{COCHRAN_QTEST} as a function
i.e. \code{prepare_test(.test = COCHRAN_QTEST)}.
}
\details{
\code{H0}: all samples come from the same distribution (equal medians, under
the assumption of equal shape). \code{H1}: at least one sample is
stochastically greater than another.
}
\section{Supported variable mapper \verb{<var_id>}s}{

\itemize{
\item \code{x_by()}: grouped Kruskal-Wallis test, with optional pairwise
comparisons. See details from \link{cqtest-xby}.
}
}

\examples{
set.seed(123)
x = sample(0:1, 45, replace = TRUE)
treatment = gl(3, 1, 45, labels = c("A", "B", "C"))
block = gl(5, 3, 45, labels = 1:5)
COCHRAN_QTEST(x_by_b(x, treatment, block))

}
\seealso{
\link{cqtest-xby}, \link{class_cq_test}, \code{\link[statim:via]{statim::via()}}, \code{\link[statim:conclude]{statim::conclude()}}
}
