% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kruskal-test-defs-xby.R
\name{kwtest-xby}
\alias{kwtest-xby}
\title{Kruskal-Wallis Test: Grouped (\code{x_by})}
\description{
The \code{x_by} implementation tests whether a continuous variable's
distribution differs across the levels of one or more grouping
variables. It accepts one or more grouping variables via \code{\link[=x_by]{x_by()}},
running one Kruskal-Wallis test per grouping variable.
}
\section{Arguments}{

\code{kwtest_def_xby}'s baseline \code{fn} takes no arguments beyond \code{.proc}.
Nothing is currently passed through \code{...} in \code{\link[=KW_TEST]{KW_TEST()}} or \code{\link[statim:via]{statim::via()}} for
the default path, yet.
}

\section{Variants}{

\describe{
\item{\code{"pairwise"}}{Runs the default Kruskal-Wallis test, then all
pairwise group comparisons using a rank-based z-test with tie
correction. Accepts one additional argument:
\describe{
\item{\code{p_adj_method}}{P-value adjustment method for the pairwise
comparisons, passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}. Must be one of
\link[stats:p.adjust]{stats::p.adjust.methods}. Default \code{"holm"}.}
}
Requires at least two groups; errors otherwise.}
}
}

\section{Grouped Kruskal-Wallis default class}{

By default, returns a \link{class_kw_test} object. The \code{pairwise} variant
returns a plain list (\code{kw_test}, \code{comps}) with its own registered
\code{print} method — it does not return \code{class_kw_test}, so it does not
inherit \code{\link[statim:auto_tidy]{statim::auto_tidy()}} automatically. \code{\link[statim:making_tidy]{statim::making_tidy()}} is registered
separately for the \code{pairwise} path via \code{.x@data$comps}.
}

\examples{
set.seed(123)
x = rcauchy(50, 1, 1.5)
g = sample(letters[1:5], size = 50, replace = TRUE)
KW_TEST(x_by(x, g))

KW_TEST(x_by(x, g)) |> via("pairwise")

KW_TEST(x_by(x, g)) |> via("pairwise", p_adj_method = "bonferroni")

}
\seealso{
Other kwtest-implementations: 
\code{\link{kwtest-on}}
}
\concept{kwtest-implementations}
\keyword{internal}
