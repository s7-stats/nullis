% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jt-test-defs-xby.R
\name{jttest-xby}
\alias{jttest-xby}
\title{Jonckheere-Terpstra Test: Grouped (\code{x_by})}
\description{
The \code{x_by} implementation tests whether a continuous variable trends
monotonically across the levels of one or more grouping variables. It
accepts one or more grouping variables via \code{\link[=x_by]{x_by()}}, running one
Jonckheere-Terpstra test per grouping variable.
}
\section{Arguments}{

\code{jttest_def_xby}'s baseline \code{fn} passes these straight through to the
compiled \code{jonckheere_terpstra_test()} C++ backend:
\describe{
\item{\code{alternative}}{One of \code{"two.sided"}, \code{"increasing"}, or
\code{"decreasing"}. Default \code{"two.sided"}. Invalid values are rejected
by the backend itself.}
\item{\code{approximate}}{Force the normal approximation instead of the
exact null distribution. Default \code{FALSE}. The backend only uses the
exact distribution when there are 50 or fewer observations and this
is \code{FALSE}.}
}
}

\section{Grouping requirement}{

Every grouping variable passed to \code{\link[=x_by]{x_by()}} must be an \strong{ordered
factor}. Jonckheere-Terpstra tests a \emph{directional} trend across group
levels — unlike Kruskal-Wallis, group order changes what the test
means, so an unordered factor or plain character vector is refused
rather than silently sorted alphabetically.
}

\section{Grouped Jonckheere-Terpstra default class}{

Always returns a \link{class_jt_test} object, which inherits
\link[statim:class_stat_infer]{statim::class_stat_infer} and so picks up \code{\link[statim:auto_tidy]{statim::auto_tidy()}} automatically.
There is no separate \code{making_tidy()} registration needed for this path.
}

\examples{
set.seed(123)
x = rcauchy(50, 1, 1.5)
g = factor(
    sample(letters[1:3], size = 50, replace = TRUE),
    levels = c("a", "b", "c"),
    ordered = TRUE
)
JT_TEST(x_by(x, g))

JT_TEST(x_by(x, g), alternative = "increasing")

JT_TEST(x_by(x, g), approximate = TRUE)

}
\concept{jttest-implementations}
\keyword{internal}
