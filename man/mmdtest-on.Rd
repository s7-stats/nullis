% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mood-median-test-on.R
\name{mmdtest-on}
\alias{mmdtest-on}
\title{Mood's Median Test (\code{on})}
\description{
The \code{on} implementation runs Mood's median test via a compiled C++
backend (\code{mood_median_test_cpp()}) directly on \code{.proc$data}, without
going through an R-level \code{x_by()} grouping split.
}
\section{Arguments}{

\code{mmdtest_def_on}'s baseline \code{fn} takes \code{.proc} plus optional
\code{custom_median} and \code{display_ct}, forwarded through \code{...} in
\code{\link[=MEDIAN_TEST]{MEDIAN_TEST()}} or \code{\link[statim:via]{statim::via()}}. When \code{.proc$data} holds a single group,
\code{custom_median} should almost always be supplied — see the note in
\link{MEDIAN_TEST}.
}

\section{Variants}{

None. \code{mmdtest_def_on} declares only a \code{base} baseline — no \code{variant()}
entries.
}

\section{Mood's Median Test default class}{

Returns a \link{class_median_test} object, same as \code{mmdtest_def_xby}'s
\code{base}. \code{vars} is set to \code{"on"} — there is no grouping variable on this
path, since \code{.proc$data} is a plain list of groups rather than an
\code{x_by()} split. \code{cont_tab} holds a single contingency table, wrapped in
a one-element list to match \code{mmdtest_def_xby}'s convention.
}

\examples{
set.seed(123)
x = rcauchy(50, 1, 1.5)
y = rcauchy(50, 3, 1.5)
MEDIAN_TEST(on(x, y))

# single group against a hypothesized median
MEDIAN_TEST(on(x), custom_median = 1)

}
\seealso{
Other mmdtest-implementations: 
\code{\link{mmdtest-xby}}
}
\concept{mmdtest-implementations}
\keyword{internal}
