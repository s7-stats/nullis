% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/friedman-test-defs.R
\name{friedman-xby}
\alias{friedman-xby}
\title{Friedman Rank Sum Test: Blocked (\code{x_by_b})}
\description{
The \code{x_by_b} implementation tests whether treatment effects differ
across k >= 2 related conditions measured on the same block (e.g.
repeated measures on the same subject, or a randomized block design).
It accepts a continuous response, a treatment/grouping variable, and a
blocking variable via \code{\link[=x_by_b]{x_by_b()}}.
}
\section{Arguments}{

\code{friedman_def_xby}'s baseline \code{fn} takes no arguments beyond \code{.proc} —
nothing is currently passed through \code{...} in \code{\link[=FRIEDMAN_TEST]{FRIEDMAN_TEST()}} or
\code{\link[statim:via]{statim::via()}}.
}

\section{Variants}{

None. \code{friedman_def_xby} declares only a \code{base} baseline in its
\code{\link[statim:agendas]{statim::agendas()}} — no \code{variant()} entries.
}

\section{Grouped Friedman default class}{

No S7 wrapper exists yet. The baseline returns whatever
\code{friedman_test_group()} returns, unwrapped — a plain list with
\code{statistic}, \code{df}, \code{p_value}. There is no \code{print()} method and no
\code{\link[statim:auto_tidy]{statim::auto_tidy()}} dispatch.
}

\examples{
set.seed(123)
x = rnorm(30)
g = rep(letters[1:3], 10)
b = rep(1:10, each = 3)
FRIEDMAN_TEST(x_by_b(x, g, b))

}
\concept{friedman-implementations}
\keyword{internal}
